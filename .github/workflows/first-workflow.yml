name: Test pipeline

on: push

jobs:
    firstjob:
        runs-on: ubuntu-latest

        steps:
            - name: checkout code
              uses: actions/checkout@v3

            - name: Test
              run: node test.js
             
            
