name: Test pipeline

on: push

jobs:
    firstjob:
        runs-on: ubuntu-ubutu

        steps:
            - uses: actions/checkout@v3

            - name: Run test
              run: node test.js
             
            
